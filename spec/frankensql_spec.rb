require 'spec_helper'

describe Frankensql do
  it 'has a version number' do
    expect(Frankensql::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
